/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed

# Processed Service Worker (root scope for Push on backend-served apps).
# Source template lives in public/sw-template.js (committed, never overwritten by build).
# Generated processed Service Worker (has injected manifest + root scope).
# Source template is public/sw-template.js (committed).

